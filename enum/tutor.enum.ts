export enum RequestStatus {
    active="active",
    pending="pending",
    rejected="rejected"
}

export enum GroupStatus {
    active="active",
    disabled="disabled",
    locked="locked"
}

export enum GroupMemberStatus {
    active='active',
    disabled='disabled'
}
